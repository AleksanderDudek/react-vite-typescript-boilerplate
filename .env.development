# Development environment overrides
# These are non-secret, safe-to-commit defaults for development.
# Secrets should go in .env.local (gitignored).

VITE_APP_TITLE=Pexels Explorer (Dev)
VITE_API_BASE_URL=https://api.pexels.com
VITE_PORT=5173
