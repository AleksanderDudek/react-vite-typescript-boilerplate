# Production environment config
# Non-secret values only. Secrets must be injected at build time
# via CI/CD pipeline environment variables.

VITE_APP_TITLE=Pexels Explorer
VITE_API_BASE_URL=https://api.pexels.com
